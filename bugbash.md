Very few bugs so far. 



get_head_state() also does nothing, but I don't know how much people are going to need that. I don't believe the python library has any equivalent.

Have not tested any screen methods yet.

IO seems blocked in the terminal view; that's probably not much of an issue. Acceptable.

Undoing segments is animated, but not along the line; the segment disappears and then the turtle moves backwards.

Don't understand the behavior of `towards()` but it may work as intended. 

Everything else seems to work.